%# vim:set ft=mason:
<%args>
$consumer_guid
$ledger_guid
</%args>

<%perl>
$m->mp_request(
  POST => "/ledger/guid/$ledger_guid/consumers/guid/$consumer_guid/cancel", {}
);
$m->redirect('/ledger/' . $ledger_guid);
</%perl>
