%# vim:set ft=mason:
<%args>
$username
</%args>
<%init>
my $ledger = $m->mp_request(GET => "/ledger/xid/" . username_xid($username));
</%init>

<h2>Your Dashboard!</h2>
<h3>currently logged in as <% $username %>

<pre>
<% Data::Dumper->Dump([ $ledger ]) |h %>
</pre>
