<%args>
$params
</%args>
<%perl>
my $ledger = $m->mp_request(
  POST => '/ledgers' => {
    name => $params->{signup}{name},
    email_addresses => $params->{signup}{email_addresses},
    consumers => {
      "unalay://username/$params->{signup}{username}" => {
        template => 'username'
      },
    },
  },
);
$m->redirect('/dashboard?guid=' . $ledger->{value}{guid});
</%perl>
