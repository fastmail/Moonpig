<%args>
$params
</%args>
<%perl>
my $username = $params->{login}{username};

my $ledger = $m->mp_request(GET => "/ledger/xid/unalay:username:$username");
$m->redirect('/dashboard?guid=' . $ledger->{value}{guid});
</%perl>
